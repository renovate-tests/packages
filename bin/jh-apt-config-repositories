#!/usr/bin/env bash

#
# Activate the local copy of the repo
#

ROOT="$( realpath "$( dirname "$( dirname "${BASH_SOURCE[0]}" )" )" )"

echo "deb [trusted=yes] file://$ROOT/repo /" > /etc/apt/sources.list.d/jehon-package-repo.list
