#!/usr/bin/env bash

case "$1" in
	configure)
		# Because fakeroot on sysv is not working...
		# update-alternatives --set fakeroot /usr/bin/fakeroot-tcp

		;;
	abort-upgrade|abort-remove|abort-deconfigure)
	    ;;

	*)
	    echo "postinst called with unknown argument \`$1'" >&2
	    exit 1
	    ;;
esac

exit 0
